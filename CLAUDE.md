@AGENTS.md

## Comandi utili
- `npm test` - esegue i test (vitest run)
- `npm run update` - esegue lo scraper una volta (richiede `.env`, va lanciato con `npm run`, non `tsx` diretto)
- `npm run cron` - avvia lo scraper schedulato (node-cron)
