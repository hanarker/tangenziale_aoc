/**
 * Interruttore unico per Google AdSense (script publisher tag + AdSlot).
 * Il sito è stato bocciato in fase di revisione AdSense (luglio 2026): tutto il
 * codice resta al suo posto, pronto per essere riattivato quando l'account
 * sarà approvato, semplicemente rimettendo questo flag a `true`.
 */
export const ADS_ENABLED = false
